rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証済みユーザーのみ読み書き可能 (Phase 3 でロールベースに強化予定)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
