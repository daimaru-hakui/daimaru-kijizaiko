import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    fonts: {
        heading: `Zen Kaku Gothic New,cursive,"Hiragino Kaku Gothic ProN"`,
        body: `Zen Kaku Gothic New, Noto Sans JP,cursive,"Hiragino Kaku Gothic ProN"`,
    },
})

export default theme
